"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def lis(nums: list[int]) -> int:
    """Return the length of the longest strictly increasing subsequence of nums.

    A subsequence is not required to be contiguous. "Strictly increasing" means
    each element is strictly greater than the previous.
    For an empty list, return 0.
    Must run in O(n^2) time using a 1-D DP array.
    """
    raise NotImplementedError
