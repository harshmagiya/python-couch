"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def bsearch_index(arr: list[int], target: int) -> int:
    """Return an index i such that arr[i] == target, or -1 if absent.

    arr is sorted in non-decreasing order. When target appears multiple
    times, any valid index is acceptable (the next exercise narrows this).
    Must run in O(log n) and must not use the bisect module.
    """
    raise NotImplementedError
