"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def subsets(nums: list[int]) -> list[list[int]]:
    """Return the power set of `nums` (distinct ints) in deterministic order.

    Order: by length (empty first, full last), then lexicographically.
    Use backtracking. No itertools.combinations or bit-mask trick.
    """
    raise NotImplementedError
