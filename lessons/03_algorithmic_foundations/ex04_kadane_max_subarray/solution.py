"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def max_subarray_sum(nums: list[int]) -> int:
    """Return the maximum sum of any non-empty contiguous subarray of nums.

    For all-negative input, return the largest (least-negative) single element.
    If nums is empty, raise ValueError.

    Must run in O(n) time and O(1) extra space (single pass; no DP table).
    """
    raise NotImplementedError
