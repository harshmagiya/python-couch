"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def pair_sum_sorted(nums: list[int], target: int) -> tuple[int, int] | None:
    """Return (i, j) with i < j and nums[i] + nums[j] == target, or None.

    nums is sorted in non-decreasing order. Must run in O(n) using
    the two-pointer technique. No dict/set "complement" lookup.
    """
    raise NotImplementedError
