"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def first_and_last(arr: list[int], target: int) -> tuple[int, int]:
    """Return (first_index, last_index) of target in sorted arr.

    Returns (-1, -1) if target is not present. Must run in O(log n)
    and must not use the bisect module.
    """
    raise NotImplementedError
