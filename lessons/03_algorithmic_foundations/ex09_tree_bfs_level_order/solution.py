"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def level_order(root):
    """Return a list of lists of node values, one inner list per level (top-down).

    The Node class is defined in test_solution.py and has .val, .left, .right.
    An empty tree (root is None) returns [].
    Must run in O(n) time using a queue.
    """
    raise NotImplementedError
