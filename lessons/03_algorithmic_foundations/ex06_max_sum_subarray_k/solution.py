"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def max_sum_subarray_k(nums: list[int], k: int) -> int:
    """Return the maximum sum of any contiguous length-k subarray of nums.

    Raises ValueError when k > len(nums) or (len(nums) == 0 and k > 0).
    Must run in O(n) time using a sliding window (O(1) per slide, no
    sum(...) recomputation inside the loop).
    """
    raise NotImplementedError
