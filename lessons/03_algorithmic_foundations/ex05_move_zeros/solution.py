"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def move_zeros(nums: list[int]) -> list[int]:
    """Move all 0s to the end of nums in place, preserving non-zero order.

    Mutates nums and also returns it. Must run in O(n) time and O(1)
    extra space (no helper list of length n).
    """
    raise NotImplementedError
