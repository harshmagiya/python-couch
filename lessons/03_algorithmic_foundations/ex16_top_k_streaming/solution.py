"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def top_k(nums: list[int], k: int) -> list[int]:
    """Return the k largest elements of nums, in descending order.

    Use a min-heap of size k (heapq). O(n log k) time, O(k) extra space.
    For k = 0, return []. For k < 0, raise ValueError.
    For empty nums with k > 0, raise ValueError.
    """
    raise NotImplementedError
