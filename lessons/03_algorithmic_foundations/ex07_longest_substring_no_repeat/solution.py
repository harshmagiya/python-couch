"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def longest_substring_no_repeat(s: str) -> int:
    """Return the length of the longest substring of s with no repeated character.

    Must run in O(n) time using a variable sliding window and a
    last-seen-index dict. Unicode characters count as one.
    """
    raise NotImplementedError
