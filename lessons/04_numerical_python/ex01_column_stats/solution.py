"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def column_stats(data: list[list[float]]) -> tuple[list[float], list[float]]:
    """Return (column_means, column_population_stds) for a 2-D dataset.

    - data is shape (n_rows, n_cols); n_rows >= 1, n_cols >= 1.
    - Population std (ddof=0).
    - If data is empty, raise ValueError.
    - Implementation must be vectorised; no Python for/while loops.
    """
    raise NotImplementedError
