"""Exercise solution.

You should only edit this file.
"""

from __future__ import annotations


def subtract_row_mean(x: list[list[float]]) -> list[list[float]]:
    """Return a row-centred copy of x.

    - For each row, subtract the mean of that row from every element.
    - Use numpy broadcasting (no np.tile / np.repeat).
    - No Python for/while loops.
    - If x is empty, raise ValueError.
    """
    raise NotImplementedError
